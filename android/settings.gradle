pluginManagement {
  def projectRoot = rootDir.parentFile

  def reactNativeGradlePlugin = file(
    providers.exec {
      workingDir(projectRoot)
      commandLine("node", "--print", "require.resolve('@react-native/gradle-plugin/package.json', { paths: [require.resolve('react-native/package.json')] })")
    }.standardOutput.asText.get().trim()
  ).getParentFile().absolutePath
  includeBuild(reactNativeGradlePlugin)

  def expoPluginsPath = file(
    providers.exec {
      workingDir(projectRoot)
      commandLine("node", "--print", "require.resolve('expo-modules-autolinking/package.json', { paths: [require.resolve('expo/package.json')] })")
    }.standardOutput.asText.get().trim()
  ).parentFile.absolutePath + "/android/expo-gradle-plugin"
  includeBuild(expoPluginsPath)
}

plugins {
  id("com.facebook.react.settings")
  id("expo-autolinking-settings")
}

// Fix: @react-native-async-storage/async-storage 2.x declares
//   configurations { compileClasspath }
// BEFORE applying 'com.android.library'.  The Groovy configurations {} DSL creates a
// legacy configuration with canBeConsumed=true, which Gradle attribute-matching then
// selects as an artifact source when resolving :app's compile classpath.  Its
// artifact-transform providers are never initialised, causing:
//   "Cannot query the value of this provider because it has no value available"
//
// addProjectEvaluationListener.beforeEvaluate fires for every project JUST BEFORE its
// build.gradle runs, regardless of --configure-on-demand.  We register a
// whenObjectAdded hook that immediately sets canBeConsumed=false on any compileClasspath
// created with the wrong role, preventing Gradle from selecting it as an artifact source.
gradle.addProjectEvaluationListener(new ProjectEvaluationListener() {
  void beforeEvaluate(Project p) {
    p.configurations.whenObjectAdded { config ->
      if (config.name == 'compileClasspath' && config.canBeConsumed) {
        config.canBeConsumed = false
      }
    }
  }
  void afterEvaluate(Project p, ProjectState s) {}
})

// Also disable configure-on-demand: Expo CLI always passes --configure-on-demand;
// this overrides it so every project is configured eagerly, eliminating the
// lazy-evaluation timing window entirely.
gradle.startParameter.configureOnDemand = false

expoAutolinking.projectRoot = rootDir.parentFile

extensions.configure(com.facebook.react.ReactSettingsExtension) { ex ->
  ex.autolinkLibrariesFromCommand()
}
expoAutolinking.useExpoModules()

rootProject.name = 'CrossStitcher'

expoAutolinking.useExpoVersionCatalog()

include ':app'
includeBuild(expoAutolinking.reactNativeGradlePlugin)
